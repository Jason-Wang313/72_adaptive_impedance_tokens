# Reviewer Attacks

- The learned gain regressor is a stronger, simpler baseline and beats the token method by a wide paired margin.
- The full token memory/update mechanism is not supported because `token_no_memory` performs better in the ablation.
- The stress sweep has a low-stress force-undertracking artifact; continuous metrics are reported, but it is not a polished benchmark contribution.
- There is no real robot, public benchmark, or qualitative rollout evidence.
- The related-work area is crowded with adaptive impedance, admittance switching, and force-tracking controllers.

Response: accept the rejection. The correct action is `KILL_ARCHIVE`, not claim shaping.
